# MTH.D-2026-1059 — definition `WMSpec.tensorVarCharacterized`

`def` in `WMSpec.GuardFamilies`; exercisable. Author-provided; Dictionary interrogation pending.

```
WMSpec.tensorVarCharacterized : (n : ℕ) →
  WMSpec.CharacterizedGuard (Spec.Tensor TorchLean.Floats.IEEE754.IEEE32Exec (Spec.Shape.dim n Spec.Shape.scalar))
```

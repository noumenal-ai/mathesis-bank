# MTH.D-2026-1069 — definition `WMSpec.tensorVarGuard`

`def` in `WMSpec.GuardFamilies`; exercisable. Author-provided; Dictionary interrogation pending.

```
WMSpec.tensorVarGuard : {n : ℕ} → Spec.Tensor TorchLean.Floats.IEEE754.IEEE32Exec (Spec.Shape.dim n Spec.Shape.scalar) → ℝ
```

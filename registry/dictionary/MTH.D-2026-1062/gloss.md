# MTH.D-2026-1062 — definition `WMSpec.ObjectiveInvariant`

`def` in `WMSpec.ObjectiveInvariance`; exercisable. Author-provided; Dictionary interrogation pending.

```
WMSpec.ObjectiveInvariant : {Θ : Type u_1} → {X : Type u_2} → {M : Type u_3} → {V : Type u_4} → (Θ → X → M → V) → (X → X) → Prop
```

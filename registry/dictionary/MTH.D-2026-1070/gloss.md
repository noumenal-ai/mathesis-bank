# MTH.D-2026-1070 — definition `WMSpec.MonotoneDeviation`

`def` in `WMSpec.ObjectiveInvariance`; exercisable. Author-provided; Dictionary interrogation pending.

```
WMSpec.MonotoneDeviation : {Θ : Type u_1} → {X : Type u_2} → {M : Type u_3} → {k : ℕ} → (Fin k → Θ → X → M → ℝ) → (X → X) → Prop
```

# MTH.D-2026-1011 — definition `WMSpec.matchedObjective`

`def` in `WMSpec.MatchedObjective`; exercisable. Author-provided; Dictionary interrogation pending.

```
WMSpec.matchedObjective : {Θ : Type u_1} →
  {X : Type u_2} → {M : Type u_3} → {k : ℕ} → (Θ → X → M → ℕ) → (Fin k → X → ℝ) → (Fin k → ℝ) → Θ → X → M → ℝ
```

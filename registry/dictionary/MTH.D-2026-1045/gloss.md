# MTH.D-2026-1045 — definition `WMSpec.enrichedObjective`

`def` in `WMSpec.MaskIndexedInvariance`; exercisable. Author-provided; Dictionary interrogation pending.

```
WMSpec.enrichedObjective : {Θ : Type u_1} → {X : Type u_2} → {M : Type u_3} → (Θ → X → M → ℕ) → (X → ℕ) → Θ → X → M → ℕ
```

Whenever a Boolean checker accepts a quotient certificate, the certified relation is a fixed point of the partition-refinement step. The engine's claim that its partition has stabilized is itself certificate-checkable.

**Cites** `WMSpec.Compiler.quotCert_sound` — the corpus theorem this result restates and discharges.

**Original contribution.** The corpus lemma is soundness of the quotient checker; the thesis frames it as making the engine's stabilization claim externally verifiable by a small Boolean certificate.

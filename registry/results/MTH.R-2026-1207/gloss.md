The partition an executable program computes is, by definition, the bisimilarity relation determined by its observations and action-labeled transitions. The runnable engine and the mathematical definition are one declaration, not two objects kept in sync.

**Cites** `WMSpec.Compiler.quotient_eq_causalRel` — the corpus theorem this result restates and discharges.

**Original contribution.** The corpus lemma is a definitional equality; the thesis frames it as collapsing the usual gap between an executable engine and the mathematical object it is meant to compute.

# MTH.R-2026-1157 — result `WMSpec.w1_toDyadic` [T1]

`theorem` in `WMSpec.ExecutableGuard`; polarity universal. Discharges MTH.C-2026-1157. Kernel-verified (whole-theory replay); axioms ['propext', 'Quot.sound'].

```
WMSpec.w1_toDyadic : (TorchLean.Floats.IEEE754.IEEE32Exec.ofBits 1065353217).toDyadic? = some { sign := false, mant := 8388609, exp := -23 }
```

If two inputs land close together in the encoder's output but the target separates them by more than the allowed Lipschitz budget, then no Lipschitz readout of the target through the encoder can exist. The audit's rejection is a theorem of the metric geometry, so a failed audit is a proof rather than a warning.

**Cites** `WMSpec.Compiler.geometry_refutes` — the corpus theorem this result restates and discharges.

**Original contribution.** The corpus lemma is the geometric refutation inequality; the thesis frames it as turning an executable audit's rejection into a proof that no readout exists.

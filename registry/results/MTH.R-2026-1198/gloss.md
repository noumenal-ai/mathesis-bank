Every bow model whose interventional query is not yet identified admits an intervention that genuinely changes the model and strictly raises the objective by exactly its current width (the size of its identification gap). A system is never stuck except through poverty of its intervention menu. Holds for every bow over every finite background type.

**Cites** `WMSpec.Transform.cz_omega_no_freeze` — the corpus theorem this result restates and discharges.

**Original contribution.** The raw lemma is an existence-of-improving-intervention result; the thesis-layer statement identifies it as the general progress guarantee underlying the certified self-modification ladder, with the improvement equal to the width.

# MTH.R-2026-1140 — result `WMSpec.w0_toDyadic` [T1]

`theorem` in `WMSpec.ExecutableGuard`; polarity universal. Discharges MTH.C-2026-1140. Kernel-verified (whole-theory replay); axioms ['propext', 'Quot.sound'].

```
WMSpec.w0_toDyadic : (TorchLean.Floats.IEEE754.IEEE32Exec.ofBits 1065353216).toDyadic? = some { sign := false, mant := 8388608, exp := -23 }
```

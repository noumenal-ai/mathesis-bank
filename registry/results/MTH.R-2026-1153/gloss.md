# MTH.R-2026-1153 — result `TLT.NonIdentifiability.Executed.x₁_toDyadic` [T0]

`theorem` in `WMSpec.NonIdentifiability.ExecutedWitness`; polarity universal. Discharges MTH.C-2026-1153. Kernel-verified (whole-theory replay); axioms ['propext', 'Quot.sound'].

```
TLT.NonIdentifiability.Executed.x₁_toDyadic : TLT.NonIdentifiability.Executed.x₁.toDyadic? = some { sign := false, mant := 8388608, exp := -23 }
```

# MTH.R-2026-1106 — result `WMSpec.Flow.Kin.mk.sizeOf_spec` [T0]

`theorem` in `WMSpec.Flow.FlowArchitecture`; polarity universal. Discharges MTH.C-2026-1106. Kernel-verified (whole-theory replay); axioms ['propext', 'Classical.choice', 'Quot.sound'].

```
WMSpec.Flow.Kin.mk.sizeOf_spec : ∀ (x v : ℝ), sizeOf { x := x, v := v } = 1 + sizeOf x + sizeOf v
```

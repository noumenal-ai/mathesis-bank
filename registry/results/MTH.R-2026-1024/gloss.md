# MTH.R-2026-1024 — result `WMSpec.varQ_const` [T0]

`theorem` in `WMSpec.MixedFamilies`; polarity universal. Discharges MTH.C-2026-1024. Kernel-verified (whole-theory replay); axioms ['propext', 'Classical.choice', 'Quot.sound'].

```
WMSpec.varQ_const : ∀ {n : ℕ} (c : ℚ), (WMSpec.varQ fun x => c) = 0
```

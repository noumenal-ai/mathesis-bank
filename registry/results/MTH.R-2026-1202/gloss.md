Two adjacent float32 bit patterns, once executed, sit at a real distance of exactly one unit in the last place (1/8388608). The separation between the outputs of an executed encoder is an exact real number, computed with no rounding story.

**Cites** `TLT.NonIdentifiability.Executed.exec_latent_gap` — the corpus theorem this result restates and discharges.

**Original contribution.** The corpus lemma is a concrete distance computation; the thesis restates it as evidence that an executed encoder's output gap is an exact real quantity, tying execution to the section's exact-geometry claims.

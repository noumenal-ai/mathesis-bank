# MTH.R-2026-1047 — result `TLT.NonIdentifiability.Executed.x₂_toDyadic` [T0]

`theorem` in `WMSpec.NonIdentifiability.ExecutedWitness`; polarity universal. Discharges MTH.C-2026-1047. Kernel-verified (whole-theory replay); axioms ['propext', 'Quot.sound'].

```
TLT.NonIdentifiability.Executed.x₂_toDyadic : TLT.NonIdentifiability.Executed.x₂.toDyadic? = some { sign := false, mant := 8388609, exp := -23 }
```

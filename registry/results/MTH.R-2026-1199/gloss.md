The interventional-query spread across all models that share the reference model's full observational reads is exactly 1/4, proved by matching upper and lower bounds. Together with its neighbors 1/2 and 0, it fixes the ladder's objective values as proved spreads rather than chosen numbers.

**Cites** `WMSpec.Transform.rung1_width` — the corpus theorem this result restates and discharges.

**Original contribution.** The raw lemma is a single width equation; the thesis-layer statement anchors it as one certified rung value that, with its neighbors, pins the entire self-modification ladder's objective to proved geometry.

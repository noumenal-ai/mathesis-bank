For a single qubit evolving under its own unitary dynamics, the coarsest distinction between states that full tomographic measurement can support is exactly the range of the moment map. Only the measurement setup is supplied; the resulting state geometry is proved equal to this independently defined object.

**Cites** `WMSpec.QubitSystem.tomoQuotientEquivMomentCone` — the corpus theorem this result restates and discharges.

**Original contribution.** The raw lemma is a bare type equivalence for a qubit; the thesis-layer statement fixes the scope explicitly (the dynamics is supplied as the question, the metric/cone/signature is never supplied) and casts it as the quantum leg of one cross-field protocol.

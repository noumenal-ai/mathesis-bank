# MTH.R-2026-1136 — result `WMSpec.enrichedGuard_nonvacuous` [T0]

`theorem` in `WMSpec.MaskIndexedInvariance`; polarity existential. Discharges MTH.C-2026-1136. Kernel-verified (whole-theory replay); axioms ['propext'].

```
WMSpec.enrichedGuard_nonvacuous : ¬WMSpec.ObjectiveInvariant (WMSpec.enrichedObjective (fun x x_1 x_2 => 0) fun x => if x = true then 1 else 0) not
```

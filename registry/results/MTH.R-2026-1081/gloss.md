# MTH.R-2026-1081 — result `WMSpec.toRat_w_ne` [T0]

`theorem` in `WMSpec.ExecutableGuard`; polarity existential. Discharges MTH.C-2026-1081. Kernel-verified (whole-theory replay); axioms ['propext', 'Classical.choice', 'Quot.sound'].

```
WMSpec.toRat_w_ne : WMSpec.toRat (WMSpec.w.2 0) ≠ WMSpec.toRat (WMSpec.w.2 1)
```
